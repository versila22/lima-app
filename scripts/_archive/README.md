# Archived scripts

One-shot or debugging scripts kept here for reference. They've already done
their job (data migrations, fix scripts, debug investigations) but might be
useful as templates if a similar one-off task comes up later.

If you're sure a script is dead weight, just `git rm` it — git history keeps it.
