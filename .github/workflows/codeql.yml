# CodeQL disabled — re-enable when ready to address security findings
# Original workflow kept for reference but not triggered
name: "CodeQL Advanced"
on:
  workflow_dispatch:  # manual trigger only
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CodeQL disabled"
